<h1>Shura's Blog Post</h1>
<% @posts.each do |post| %>
  <div class="well">
    <h3><%= post.title %> </h3>
    <p><%= post.body %> </p>
    <%= link_to "Go to post", post_path(post), :class => 'btn btn-default' %>
  </div>
 <% end %>